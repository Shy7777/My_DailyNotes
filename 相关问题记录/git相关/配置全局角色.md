配置你的 Git 邮箱（替换成你自己的邮箱，比如 GitHub/Gitee 绑定的邮箱）

	git config --global user.email "你的邮箱@example.com"
配置你的 Git 用户名（替换成你想显示的名称，比如你的昵称/账号名） 

	git config --global user.name "你的用户名"
执行如下命令，配置出现配置信息说明成功

	git config --global --list

![[Pasted image 20260301223616.png]]